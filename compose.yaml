services:
  xdcc-extractor:
    build: .
    container_name: xdcc-extractor
    restart: unless-stopped

    environment:
      TZ: Europe/Berlin

    volumes:
      - /media/HDD3/XDCC_TEST:/downloads
      - ./config.docker.toml:/app/config.toml:ro
      - ./state:/state
